[project]
name = "api"
version = "1.0"
description = "Mijn basic api"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.2",
]

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "ruff>=0.15.4",
]
